*classimax-main -> 
- Home Page (for all users so they can view and rate stores).
- All you need are home, single_store.php files.

*admin-bootstrap -> 
- Admin Dashboard (All PHP files are in php folder).

*admin-conn ->
- Database Connection file and Session (for login part) file.

*Db->
- Database so you can import it into you're 'localhost/phpMyAdmin' and take a look.
